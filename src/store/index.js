import Vue from 'vue'
import Vuex from 'vuex'
const { ipcRenderer } = require('electron');

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    show: false
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
});
